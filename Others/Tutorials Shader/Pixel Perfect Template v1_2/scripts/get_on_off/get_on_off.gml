/// @description get_on_off(boolean value)
/// @param boolean value
function get_on_off(argument0) {
	//Returns a string based on if the supplied value is true or false.

	if (argument0)
	    {
	    return ("On");
	    } else {
	    return ("Off");
	    }



}
