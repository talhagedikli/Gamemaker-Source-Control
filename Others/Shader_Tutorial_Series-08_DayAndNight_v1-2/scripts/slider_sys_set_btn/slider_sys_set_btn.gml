/// @description sets up a sliders button
function slider_sys_set_btn(argument0) {
	//	used by system only
	/// @param sprite_id

	btn_sprite		= argument0;
	btn_x_padding	= round(sprite_get_width(btn_sprite) / 4);	// left and right padding so the button doesn't go all the way to the left or right



}
